import React from 'react';
import ReactDOM from 'react-dom/client';
import EasyRunLive from './EasyRunLive';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EasyRunLive />
  </React.StrictMode>
);
